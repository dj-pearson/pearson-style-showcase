# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Environment variables (SECURITY: Never commit these!)
.env
.env.local
.env.*.local
.env.production

# Supabase temp files
supabase/.temp/

# Lockfiles (keep only one)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
CODEBASE_ANALYSIS.md
generate-apple-secret.js
.env 
/deployment 


# Documentation and guides (keep these for reference)
COOLIFY_COMMAND_BUILDER_SETUP.md
DANPEARSON_SETUP_GUIDE.md
SUPABASE_COOLIFY_COMMANDS.md

# Database backups (keep out of repo)
db_cluster-*.backup.gz
backups/

# Edge Functions - ONLY ignore .env files, not the code!
danpearson-edge-functions/.env
danpearson-edge-functions/.env.*
!danpearson-edge-functions/.env.example

# Template directory (keep for reference but not needed in deployment)
edge-functions-template/
MIGRATION_STATUS.md
danpearson-edge-functions/env.example
