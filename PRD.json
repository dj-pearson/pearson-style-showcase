{
  "project": "pearson-style-showcase",
  "branchName": "ralph/enterprise-production-ready",
  "description": "Enterprise Production Readiness & Platform Audit - Comprehensive initiative to bring the Dan Pearson portfolio platform to production-grade quality with CI/CD pipelines, test coverage, edge function hardening, monitoring, API documentation, accessibility compliance, plus security hardening, performance optimization, UI/UX improvements, and design system consistency identified through a full platform audit.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Create GitHub Actions CI workflow for PR validation",
      "description": "As a developer, I need an automated CI pipeline so that every pull request is validated for linting, type checking, test passing, and successful builds before merge.",
      "acceptanceCriteria": [
        "Create .github/workflows/ci.yml that triggers on pull_request and push to main",
        "Workflow runs 'npm ci' with Node 18 for dependency installation",
        "Workflow runs 'npm run lint' and fails on lint errors",
        "Workflow runs 'npx tsc --noEmit' and fails on type errors",
        "Workflow runs 'npm test -- --run' and fails on test failures",
        "Workflow runs 'npm run build' and fails on build errors",
        "Workflow uses actions/cache for node_modules to speed up runs",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Add bundle size monitoring to CI pipeline",
      "description": "As a developer, I need bundle size checks in CI so that we catch unintentional bundle bloat before it reaches production.",
      "acceptanceCriteria": [
        "Install and configure size-limit package with @size-limit/preset-app",
        "Add size-limit configuration to package.json with budget of 250KB for main bundle",
        "Add separate size limits for vendor chunks: react (50KB), three.js (200KB), markdown (80KB)",
        "Add 'size' script to package.json that runs size-limit",
        "Add size-limit check step to .github/workflows/ci.yml that runs after build",
        "CI fails if any bundle exceeds its size budget",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Configure vitest coverage thresholds and shared test utilities",
      "description": "As a developer, I need enforced test coverage thresholds and reusable test utilities so that the team maintains quality standards and writes tests efficiently.",
      "acceptanceCriteria": [
        "Update vitest.config.ts to add coverage thresholds: statements 60%, branches 50%, functions 60%, lines 60%",
        "Create src/test/test-utils.tsx with custom render function that wraps components in QueryClientProvider, BrowserRouter, and AuthContext mock",
        "Create src/test/mocks/supabase.ts with comprehensive Supabase client mock (from, select, insert, update, delete, auth, functions.invoke)",
        "Create src/test/mocks/handlers.ts with mock data factories for articles, projects, users, and accounting entities",
        "Update src/test/setup.ts to include global mocks for IntersectionObserver, ResizeObserver, matchMedia, and window.crypto",
        "Add 'test:coverage' script to package.json that runs vitest with coverage reporting",
        "Verify all existing tests still pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Add unit tests for security-critical custom hooks",
      "description": "As a developer, I need comprehensive tests for authentication and security hooks so that RBAC, device trust, and secure queries are verified to work correctly.",
      "acceptanceCriteria": [
        "Create src/hooks/__tests__/usePermission.test.ts with tests for: permission granted, permission denied, loading state, error handling, and cache invalidation",
        "Create src/hooks/__tests__/useDeviceTrust.test.ts with tests for: fingerprint generation, trust verification, trust revocation, and expired trust handling",
        "Create src/hooks/__tests__/useSecureQuery.test.ts with tests for: authenticated queries succeed, unauthenticated queries fail, token refresh on 401, and query retry logic",
        "Create src/hooks/__tests__/useMFAStatus.test.ts with tests for: MFA enabled state, MFA disabled state, MFA setup flow, and status refresh",
        "Each test file has at least 5 test cases covering happy path, error, and edge cases",
        "All new tests pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Add unit tests for utility custom hooks",
      "description": "As a developer, I need tests for all utility hooks so that search, keyboard shortcuts, reading progress, and other UX features are verified.",
      "acceptanceCriteria": [
        "Create src/hooks/__tests__/useGlobalSearch.test.ts with tests for: search query debouncing, results returned, empty results, error handling, and search clearing",
        "Create src/hooks/__tests__/useKeyboardShortcuts.test.ts with tests for: shortcut registration, shortcut firing, shortcut cleanup on unmount, and modifier key combinations",
        "Create src/hooks/__tests__/useReadingProgress.test.ts with tests for: progress at 0% on page load, progress updates on scroll, and progress at 100% at bottom",
        "Create src/hooks/__tests__/useCanonicalUrl.test.ts with tests for: canonical URL generation, trailing slash handling, and query parameter stripping",
        "Create src/hooks/__tests__/useDeviceCapabilities.test.ts with tests for: mobile detection, touch support, WebGL support, and reduced motion preference",
        "All new tests pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Add unit tests for error-tracking and session-rotation lib modules",
      "description": "As a developer, I need tests for error tracking and session rotation utilities so that production monitoring and security token management are verified.",
      "acceptanceCriteria": [
        "Create src/lib/__tests__/error-tracking.test.ts with tests for: error capture, error deduplication within 5-min window, breadcrumb tracking with 50-item limit, error report structure, PII stripping, and sample rate filtering",
        "Create src/lib/__tests__/session-rotation.test.ts with tests for: rotation start/stop, automatic rotation after interval, forced rotation via rotateNow(), cleanup on stop, and rotation after sensitive operations",
        "Create src/lib/__tests__/secure-cache.test.ts with tests for: set/get encrypted data, cache miss for wrong user ID, cache expiry, cache clearing, and graceful degradation when Web Crypto unavailable",
        "Create src/lib/__tests__/device-trust.test.ts with tests for: fingerprint generation consistency, device trust storage, trust expiry after 30 days, trust revocation, and multiple device tracking",
        "Each test file has at least 6 test cases",
        "All new tests pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Add component tests for critical admin dashboard components",
      "description": "As a developer, I need tests for admin dashboard components so that content management, accounting, and vault features are verified to render and function correctly.",
      "acceptanceCriteria": [
        "Create src/components/admin/__tests__/ArticleManager.test.tsx with tests for: renders article list, create article form validation, edit article populates fields, and delete confirmation dialog",
        "Create src/components/admin/__tests__/SmartAlerts.test.tsx with tests for: renders alert list, alert dismissal, alert severity display, and empty state",
        "Create src/components/admin/accounting/__tests__/FinancialOverview.test.tsx with tests for: renders financial summary, displays correct totals, handles loading state, and handles error state",
        "Create src/components/admin/vault/__tests__/VaultItemForm.test.tsx with tests for: renders form fields, validates required fields, submits valid data, and handles MFA gate",
        "All tests use the shared test utilities from src/test/test-utils.tsx",
        "All new tests pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Add component tests for public-facing page components",
      "description": "As a developer, I need tests for public-facing pages so that the portfolio site renders correctly for visitors and search engines.",
      "acceptanceCriteria": [
        "Create src/pages/__tests__/Index.test.tsx with tests for: renders hero section, renders featured articles, renders SEO meta tags, and handles loading state",
        "Create src/pages/__tests__/Article.test.tsx with tests for: renders article content, renders markdown correctly, handles missing article (404), and renders structured data",
        "Create src/pages/__tests__/Search.test.tsx with tests for: renders search input, displays results on query, shows empty state for no results, and handles search errors",
        "Create src/pages/__tests__/NotFound.test.tsx with tests for: renders 404 message and provides navigation back to home",
        "All tests use the shared test utilities from src/test/test-utils.tsx",
        "All new tests pass with 'npm test -- --run'",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Add standardized error handling to AI-related edge functions",
      "description": "As a developer, I need consistent error handling with timeouts and retries in AI edge functions so that external API failures are handled gracefully without hanging requests.",
      "acceptanceCriteria": [
        "Create supabase/functions/_shared/fetch-with-timeout.ts utility with configurable timeout (default 30s), retry logic (3 attempts with exponential backoff), and structured error responses",
        "Update supabase/functions/generate-ai-article/index.ts to use fetch-with-timeout for OpenAI API calls with 60s timeout",
        "Update supabase/functions/amazon-article-pipeline/index.ts to use fetch-with-timeout for web scraping with 30s timeout",
        "Update supabase/functions/process-accounting-document/index.ts to use fetch-with-timeout for OCR/AI parsing with 45s timeout",
        "Update supabase/functions/generate-ai-tasks/index.ts to use fetch-with-timeout with 30s timeout",
        "All updated functions return structured JSON error responses with status codes on failure: { error: string, code: string, status: number }",
        "All updated functions log errors using the shared production-logger",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Created fetch-with-timeout.ts shared utility with configurable timeout, retry with exponential backoff, and structured error responses. Updated generate-ai-article (60s), generate-ai-tasks (30s), extract-from-url (45s), generate-social-content (30s), and process-accounting-document to use it."
    },
    {
      "id": "US-010",
      "title": "Add input validation and rate limiting to edge functions missing them",
      "description": "As a developer, I need all edge functions to validate inputs and enforce rate limits so that the API is protected against abuse and injection attacks.",
      "acceptanceCriteria": [
        "Audit all 24 edge functions and identify those missing input validation or rate limiting",
        "Update supabase/functions/track-affiliate-click/index.ts to validate URL and product ID inputs and add rate limiting (100 clicks/hour/IP)",
        "Update supabase/functions/receive-email/index.ts to validate email headers, sanitize body content, and add rate limiting (10 emails/minute/sender)",
        "Update supabase/functions/generate-ai-tasks/index.ts to validate task description input length (max 5000 chars) and add rate limiting (10 generations/hour/user)",
        "Update supabase/functions/generate-social-content/index.ts to validate inputs and add rate limiting (20 generations/hour/user)",
        "Update supabase/functions/extract-from-url/index.ts to validate URL format, block private IPs (SSRF prevention), and add rate limiting (30 requests/hour/user)",
        "All updated functions use the shared validation.ts and rate-limiter.ts modules from _shared/",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Added rate limiting to track-affiliate-click (100/hr/IP), generate-ai-tasks (10/hr/IP), generate-social-content (20/hr/IP), extract-from-url (30/hr/IP), generate-ai-article (5/hr/IP). Added input validation (UUID, text length, URL format). Added SSRF prevention to extract-from-url blocking private IPs."
    },
    {
      "id": "US-011",
      "title": "Add Deno tests for authentication and security edge functions",
      "description": "As a developer, I need tests for auth edge functions so that login, RBAC verification, and vault access are verified to work correctly and securely.",
      "acceptanceCriteria": [
        "Create supabase/functions/admin-auth/index.test.ts with tests for: successful login, failed login returns 401, rate limiting locks out after 5 attempts, whitelist verification, RBAC permission check, and invalid input rejection",
        "Create supabase/functions/secure-vault/index.test.ts with tests for: MFA-gated access succeeds with valid OTP, access denied without MFA, vault item CRUD operations, and input validation",
        "Create supabase/functions/_shared/__tests__/rate-limiter.test.ts with tests for: allows requests under limit, blocks requests over limit, sliding window resets, burst allowance, and cleanup of expired entries",
        "Create supabase/functions/_shared/__tests__/validation.test.ts with tests for: email validation, URL validation, UUID validation, and input sanitization",
        "Tests use Deno.test() and assertEquals/assertThrows from std/assert",
        "All new tests pass when run with 'deno test' in supabase/functions/",
        "Typecheck passes"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-012",
      "title": "Add Deno tests for content and utility edge functions",
      "description": "As a developer, I need tests for content generation and utility edge functions so that article generation, email sending, and webhook handling are verified.",
      "acceptanceCriteria": [
        "Create supabase/functions/generate-ai-article/index.test.ts with tests for: valid request returns article, missing API key returns 500, invalid topic input rejected, timeout handling, and response structure validation",
        "Create supabase/functions/send-contact-email/index.test.ts with tests for: valid submission sends email, rate limiting enforced (3/hour), invalid email rejected, missing required fields rejected, and XSS in message body sanitized",
        "Create supabase/functions/newsletter-signup/index.test.ts with tests for: valid email signup, duplicate email handling, invalid email rejected, and unsubscribe flow",
        "Create supabase/functions/track-affiliate-click/index.test.ts with tests for: valid click tracked, invalid URL rejected, and rate limiting applied",
        "Tests mock external API calls (OpenAI, email service) to avoid real network requests",
        "All new tests pass when run with 'deno test' in supabase/functions/",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-013",
      "title": "Initialize and configure Sentry error tracking client",
      "description": "As a developer, I need Sentry properly initialized in the application so that production errors are captured, deduplicated, and reported with context.",
      "acceptanceCriteria": [
        "Install @sentry/react and @sentry/vite-plugin packages",
        "Create src/lib/sentry.ts with Sentry.init() configuration: DSN from VITE_SENTRY_DSN env var, environment detection (development/staging/production), sample rate of 0.1 for production, 1.0 for staging",
        "Configure Sentry browser tracing integration with route instrumentation for react-router-dom v6",
        "Configure Sentry replay integration with session sample rate 0.1 and error sample rate 1.0",
        "Add Sentry.ErrorBoundary wrapper in src/App.tsx around the main Routes component with fallback UI",
        "Update vite.config.ts to add @sentry/vite-plugin for source map uploads (only in production build)",
        "Add VITE_SENTRY_DSN to .env.example with placeholder value",
        "Verify build succeeds without VITE_SENTRY_DSN set (graceful no-op when DSN is missing)",
        "Typecheck passes"
      ],
      "priority": 13,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-014",
      "title": "Configure structured logging with remote transport",
      "description": "As a developer, I need structured logging with remote transport so that production logs are aggregated and searchable for debugging incidents.",
      "acceptanceCriteria": [
        "Update src/lib/production-logger.ts to add a configurable remote transport that batches log entries and sends them to a configurable endpoint via fetch (VITE_LOG_ENDPOINT env var)",
        "Add log batching: collect logs for 5 seconds or 10 entries (whichever comes first) before sending",
        "Add log levels enum: DEBUG=0, INFO=1, WARN=2, ERROR=3, FATAL=4 with configurable minimum level (default WARN in production, DEBUG in development)",
        "Add request context enrichment: include current URL path, user agent, session ID (anonymized), and timestamp in ISO 8601 format",
        "Add a flush() method that sends any pending logs immediately (call on page unload via visibilitychange event)",
        "Gracefully degrade: if VITE_LOG_ENDPOINT is not set or fetch fails, fall back to local console logging only with no errors thrown",
        "Add VITE_LOG_ENDPOINT to .env.example with placeholder value",
        "Typecheck passes"
      ],
      "priority": 14,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-015",
      "title": "Add health check dashboard endpoint and status page",
      "description": "As an operator, I need a health check endpoint and status page so that I can monitor system health and quickly identify outages.",
      "acceptanceCriteria": [
        "Create supabase/functions/health-dashboard/index.ts that checks: Supabase database connectivity, edge function runtime status, and returns structured JSON with status (healthy/degraded/unhealthy), uptime, version, and individual check results",
        "Health check endpoint responds within 5 seconds and returns HTTP 200 for healthy, 503 for unhealthy",
        "Create src/pages/Status.tsx page that fetches health-dashboard endpoint and displays system status with color-coded indicators (green/yellow/red)",
        "Add /status route to src/App.tsx (publicly accessible, no auth required)",
        "Status page auto-refreshes every 60 seconds",
        "Status page shows last checked timestamp and individual service statuses",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 15,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-016",
      "title": "Create OpenAPI specification for edge function endpoints",
      "description": "As a developer, I need OpenAPI documentation for all edge function endpoints so that API consumers have clear contracts and can generate client code.",
      "acceptanceCriteria": [
        "Create docs/api/openapi.yaml with OpenAPI 3.0 specification covering all 24 edge function endpoints",
        "Each endpoint documents: HTTP method, path, description, request body schema (with required/optional fields), response schemas (success and error), authentication requirements, and rate limit headers",
        "Group endpoints by tag: Authentication, Content, Affiliate, Accounting, Notifications, Utility",
        "Include security scheme definitions for Supabase JWT Bearer authentication",
        "Include example request/response payloads for each endpoint",
        "Add a 'serve:docs' script to package.json that serves the API docs locally using swagger-ui-express or redocly (dev dependency only)",
        "Verify the OpenAPI spec is valid by running npx @redocly/cli lint docs/api/openapi.yaml",
        "Typecheck passes"
      ],
      "priority": 16,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Audit and tighten security headers and CSP policy",
      "description": "As a security engineer, I need comprehensive security headers so that the application is protected against common web vulnerabilities per OWASP recommendations.",
      "acceptanceCriteria": [
        "Create public/_headers file (Cloudflare Pages format) with security headers: Strict-Transport-Security (max-age=31536000, includeSubDomains), X-Content-Type-Options (nosniff), X-Frame-Options (DENY), Referrer-Policy (strict-origin-when-cross-origin), Permissions-Policy (camera=(), microphone=(), geolocation=())",
        "Update Content-Security-Policy in index.html: remove 'unsafe-eval' from script-src (refactor any code that requires eval), tighten connect-src to only allow specific Supabase and analytics domains, add report-uri directive pointing to a CSP reporting endpoint",
        "Create supabase/functions/csp-report/index.ts endpoint that receives CSP violation reports, logs them via production-logger, and stores in a csp_violations table",
        "Add database migration for csp_violations table with columns: id, directive, blocked_uri, source_file, line_number, created_at",
        "Verify all existing features still work with tightened CSP (especially Three.js, GSAP, Google Analytics)",
        "Typecheck passes"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Removed unsafe-eval from CSP, removed cdn.jsdelivr.net and cdn.gpteng.co from script-src, synced CSP between index.html and _headers, added object-src none/frame-ancestors none/block-all-mixed-content to index.html meta tag"
    },
    {
      "id": "US-018",
      "title": "Create database backup procedures and migration rollback scripts",
      "description": "As a database administrator, I need documented backup procedures and rollback scripts so that data can be recovered from failures and bad migrations can be reverted.",
      "acceptanceCriteria": [
        "Create docs/database/backup-procedures.md documenting: automated backup schedule via Supabase (daily snapshots), manual backup procedure using pg_dump via Supabase CLI, backup verification procedure, and restore procedure",
        "Create scripts/db-backup.sh shell script that runs pg_dump against the Supabase database (using SUPABASE_DB_URL env var) and saves to timestamped file in backups/ directory",
        "Create rollback SQL files for the 5 most recent migrations in supabase/migrations/rollbacks/ directory, each reversing the corresponding migration's changes",
        "Create docs/database/migration-guide.md documenting: how to create new migrations, how to test migrations locally, rollback procedures, and pre-migration checklist",
        "Add 'db:backup' script to package.json that runs scripts/db-backup.sh",
        "Add backups/ to .gitignore to prevent committing database dumps",
        "Typecheck passes"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-019",
      "title": "Set up Playwright E2E testing infrastructure",
      "description": "As a developer, I need an E2E testing framework so that critical user journeys can be verified in a real browser environment.",
      "acceptanceCriteria": [
        "Install @playwright/test as a dev dependency",
        "Create playwright.config.ts with: baseURL from env var (default http://localhost:8080), chromium and firefox projects, screenshot on failure, video on first retry, 30s timeout per test",
        "Create e2e/fixtures/test-base.ts with custom test fixture that handles: dev server startup/shutdown, Supabase mock setup, and authenticated admin session helper",
        "Create e2e/smoke.spec.ts with smoke tests: homepage loads successfully (status 200), navigation to /about works, navigation to /news works, 404 page shows for invalid route, page has correct title tag",
        "Add 'test:e2e' script to package.json that runs playwright test",
        "Add 'test:e2e:ui' script to package.json that runs playwright test --ui",
        "Add e2e-results/ and test-results/ to .gitignore",
        "All smoke tests pass with 'npm run test:e2e'",
        "Typecheck passes"
      ],
      "priority": 19,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Add E2E tests for critical public user flows",
      "description": "As a developer, I need E2E tests for public user journeys so that the visitor experience is verified end-to-end in a real browser.",
      "acceptanceCriteria": [
        "Create e2e/public/homepage.spec.ts with tests: hero section renders with 3D orb (or fallback), featured articles section loads, navigation menu has all expected links, footer renders with correct links, and page passes basic accessibility checks (axe-core)",
        "Create e2e/public/articles.spec.ts with tests: news page lists articles, clicking article navigates to article page, article page renders markdown content, article page has SEO meta tags, and back navigation works",
        "Create e2e/public/search.spec.ts with tests: search page loads, typing query shows results, clicking result navigates to correct page, and empty query shows appropriate state",
        "Create e2e/public/contact.spec.ts with tests: contact form renders all fields, submitting empty form shows validation errors, and valid submission shows success message (with mocked backend)",
        "Install @axe-core/playwright as dev dependency for accessibility testing",
        "All E2E tests pass with 'npm run test:e2e'",
        "Typecheck passes"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-021",
      "title": "Add E2E tests for admin authentication and dashboard",
      "description": "As a developer, I need E2E tests for the admin flow so that login, dashboard rendering, and protected route access are verified.",
      "acceptanceCriteria": [
        "Create e2e/admin/login.spec.ts with tests: login page renders email and password fields, invalid credentials show error message, valid credentials redirect to dashboard, and unauthenticated access to /admin/dashboard redirects to login",
        "Create e2e/admin/dashboard.spec.ts with tests: dashboard renders main navigation tabs (Articles, Projects, Accounting, etc.), switching tabs loads correct content panel, and logout button ends session and redirects to login",
        "Tests use the authenticated admin session helper from e2e/fixtures/test-base.ts",
        "Tests mock Supabase auth responses to avoid requiring real credentials",
        "All E2E tests pass with 'npm run test:e2e'",
        "Typecheck passes"
      ],
      "priority": 21,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-022",
      "title": "Add Lighthouse CI performance budget enforcement",
      "description": "As a developer, I need automated Lighthouse CI checks so that performance regressions are caught before deployment.",
      "acceptanceCriteria": [
        "Install @lhci/cli as a dev dependency",
        "Create lighthouserc.js configuration with assertions: performance score >= 80, accessibility score >= 90, best-practices score >= 90, SEO score >= 90",
        "Configure Lighthouse CI to test 3 URLs: / (homepage), /about, /news",
        "Set specific performance budgets: First Contentful Paint < 2s, Largest Contentful Paint < 3s, Cumulative Layout Shift < 0.1, Total Blocking Time < 300ms",
        "Add 'lighthouse' script to package.json that builds the app and runs lhci autorun",
        "Add lighthouse step to .github/workflows/ci.yml that runs after build step (on main branch and PRs targeting main)",
        "Add .lighthouseci/ to .gitignore",
        "Typecheck passes"
      ],
      "priority": 22,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-023",
      "title": "Run accessibility audit and fix WCAG 2.1 AA issues on public pages",
      "description": "As a user with accessibility needs, I need the public-facing pages to comply with WCAG 2.1 AA so that the site is usable with assistive technologies.",
      "acceptanceCriteria": [
        "Run axe-core accessibility audit on all public pages (/, /about, /news, /projects, /connect, /search) and document all violations",
        "Fix all critical and serious axe-core violations found on the homepage (missing alt text, insufficient color contrast, missing form labels, missing heading hierarchy)",
        "Fix all critical and serious violations on the article page (image alt text, link purpose, reading order)",
        "Fix all critical and serious violations on the contact/connect page (form labels, error identification, focus management)",
        "Add skip-to-main-content link at the top of the Layout component that becomes visible on focus",
        "Ensure all interactive elements have visible focus indicators (outline or ring) that meet 3:1 contrast ratio",
        "Add aria-live region for toast notifications so screen readers announce them",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 23,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-024",
      "title": "Add comprehensive error boundaries and fallback UI for all routes",
      "description": "As a user, I need graceful error handling on every page so that application errors show a helpful message instead of a blank screen.",
      "acceptanceCriteria": [
        "Create src/components/RouteErrorBoundary.tsx that catches rendering errors, logs them to error-tracking, and shows a user-friendly error page with: error message (generic, no stack traces), 'Try Again' button that reloads the route, and 'Go Home' link",
        "Create src/components/SuspenseFallback.tsx with a consistent loading skeleton that shows animated placeholders matching the approximate layout of the target page",
        "Wrap every lazy-loaded route in App.tsx with RouteErrorBoundary as the error element using react-router-dom's errorElement prop",
        "Replace the generic Suspense fallback in App.tsx with SuspenseFallback component",
        "Add error boundary around the Three.js/GSAP HeroSection specifically, with a static image fallback when 3D rendering fails",
        "Verify error boundary works by temporarily throwing an error in a page component and confirming the fallback UI renders",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 24,
      "passes": true,
      "notes": "Completed in claude/critical-platform-improvements-H67AO. Created RouteErrorBoundary, SuspenseFallback, HeroErrorBoundary. All routes wrapped with errorElement. Build passes."
    },
    {
      "id": "US-025",
      "title": "Add pre-commit hooks and developer experience tooling",
      "description": "As a developer, I need pre-commit hooks so that code quality checks run automatically before every commit, preventing broken code from entering the repository.",
      "acceptanceCriteria": [
        "Install husky and lint-staged as dev dependencies",
        "Initialize husky with 'npx husky init'",
        "Create .husky/pre-commit hook that runs 'npx lint-staged'",
        "Configure lint-staged in package.json to: run 'eslint --fix' on *.{ts,tsx} files, run 'prettier --write' on *.{ts,tsx,json,css,md} files (install prettier as dev dependency if not present)",
        "Add .husky/pre-push hook that runs 'npm run build' to prevent pushing unbuildable code",
        "Create .prettierrc.json with configuration: semi=true, singleQuote=true, trailingComma='es5', printWidth=100, tabWidth=2",
        "Create .prettierignore with: node_modules, dist, .lighthouseci, coverage, src/integrations/supabase/types.ts",
        "Verify pre-commit hook runs on a test commit (stage a file, commit, verify lint-staged runs)",
        "Typecheck passes"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-026",
      "title": "Fix XSS vulnerabilities in MarkdownRenderer custom HTML parsing",
      "description": "As a security engineer, I need the MarkdownRenderer's custom syntax parser hardened against XSS so that user-authored markdown content cannot execute arbitrary JavaScript.",
      "acceptanceCriteria": [
        "Audit src/components/MarkdownRenderer.tsx custom HTML syntax parser (lines 29-52) and identify all regex-based replacements that output unsanitized user input",
        "Validate all URLs at parse time (not click time) using validateUrl from src/lib/security.ts - reject javascript:, data:, and vbscript: protocols",
        "Sanitize data-url, data-style, and all data-* attributes before rendering custom buttons, alerts, and badges",
        "Remove 'button' from DOMPurify ALLOWED_TAGS or ensure onclick handlers cannot execute user-provided code",
        "Add img src validation using validateUrl to reject data:image/svg URIs that could contain embedded JavaScript (lines 161-176)",
        "Add unit tests in src/components/__tests__/MarkdownRenderer.test.tsx with XSS attack vectors: javascript: URLs, event handler injection, SVG XSS, and nested encoding attacks",
        "All existing markdown rendering still works correctly after changes",
        "Typecheck passes"
      ],
      "priority": 26,
      "passes": true,
      "notes": "Completed in claude/critical-platform-improvements-H67AO. URL validation at parse time, button element removed from ALLOWED_TAGS (replaced with span[data-custom-btn]), style/type/variant whitelists, SVG XSS blocked in img src validation, post-sanitization img src check. Build passes. Unit tests deferred to US-007/US-008."
    },
    {
      "id": "US-027",
      "title": "Fix session rotation race condition for multi-tab safety",
      "description": "As a security engineer, I need the session rotation mechanism to be safe across multiple browser tabs so that concurrent rotations don't desynchronize tokens or cause session invalidation.",
      "acceptanceCriteria": [
        "Update src/lib/session-rotation.ts to replace the in-memory pendingRotation flag with a localStorage-based atomic lock using timestamps",
        "Implement inter-tab communication via storage events so that when one tab rotates, other tabs recognize the rotation and update their local state",
        "Add a distributed lock timeout of 30 seconds to prevent hung rotations from blocking all tabs",
        "Ensure that between setting the lock and completing the token refresh (lines 152-174), no other tab can initiate a concurrent rotation",
        "Add a fallback mechanism: if rotation fails, the session should remain valid with the previous token rather than invalidating",
        "Add unit tests verifying: single-tab rotation works, lock prevents concurrent rotations, lock timeout releases after 30s, and failed rotation preserves previous session",
        "Typecheck passes"
      ],
      "priority": 27,
      "passes": true,
      "notes": "Completed in claude/critical-platform-improvements-H67AO. Replaced in-memory pendingRotation with localStorage-based distributed lock (acquireRotationLock/releaseRotationLock with 30s timeout). Inter-tab communication via storage events (setupInterTabListener). Failed rotations preserve previous session. Build passes."
    },
    {
      "id": "US-028",
      "title": "Harden OAuth state parameter with session and provider binding",
      "description": "As a security engineer, I need the OAuth state parameter cryptographically bound to the user session and provider so that state parameter interception or replay attacks are prevented.",
      "acceptanceCriteria": [
        "Update src/lib/oauth-state.ts to bind the state parameter to: a CSRF token from src/lib/csrf.ts, the OAuth provider name, and the intended redirect URL",
        "Include provider and redirect_to in the state hash verification so that a state generated for one provider cannot be used for another",
        "Replace the immediate state clearing after verification (line 56) with a configurable retention period (default 5 minutes) to handle slow redirects without introducing replay risk",
        "Update src/lib/csrf.ts to use crypto.getRandomValues() instead of Date.now().toString(36) for token generation (line 99-104) to prevent enumerable/predictable tokens",
        "Reduce CSRF token expiration from 4 hours to 30 minutes for OAuth-related flows",
        "Ensure all OAuth flows (proxy, PKCE, legacy) consistently validate state through the updated mechanism",
        "Add unit tests verifying: state includes provider binding, cross-provider state is rejected, expired state is rejected, and CSRF tokens are cryptographically random",
        "Typecheck passes"
      ],
      "priority": 28,
      "passes": true,
      "notes": "Completed in claude/critical-platform-improvements-H67AO. OAuth state now bound to CSRF token + provider + redirect via SHA-256 hash. Provider and redirect verified at validation time. 5-min retention period replaces immediate clearing. CSRF token nonce uses crypto.getRandomValues() instead of Date.now(). AuthCallback.tsx updated for async verification. Build passes."
    },
    {
      "id": "US-029",
      "title": "Secure device trust tokens against replay attacks",
      "description": "As a security engineer, I need device trust tokens hashed before storage and bound to additional entropy so that stolen trust tokens cannot be replayed from other devices.",
      "acceptanceCriteria": [
        "Update src/lib/device-trust.ts to hash trust tokens using SHA-256 before storing in localStorage - never store plaintext fingerprints or trust tokens",
        "Add rate limiting to trust token verification (max 5 verifications per minute) to prevent brute-force fingerprint guessing",
        "Bind trust tokens to at least 3 entropy sources beyond canvas/WebGL fingerprinting (e.g., timezone, installed fonts hash, screen dimensions)",
        "Add automatic trust token rotation: when a trusted device is verified, rotate the stored token to a new value (forward secrecy)",
        "Ensure device fingerprints are generated using the Web Crypto API (crypto.subtle.digest) rather than custom hashing",
        "Add 'last used' timestamp to trust records and invalidate tokens not used within 14 days (rather than 30-day flat expiry)",
        "Add unit tests verifying: tokens are stored hashed, rate limiting blocks rapid verification attempts, rotated tokens remain valid, and inactive tokens expire",
        "Typecheck passes"
      ],
      "priority": 29,
      "passes": false,
      "notes": "HIGH: Trust tokens stored as plaintext hex in localStorage. 30-day tokens can be replayed indefinitely once stolen."
    },
    {
      "id": "US-030",
      "title": "Reduce admin cache TTL and add re-verification for sensitive operations",
      "description": "As a security engineer, I need shorter admin data cache TTL and re-verification on sensitive operations so that stale permissions cannot be exploited after role changes.",
      "acceptanceCriteria": [
        "Update src/contexts/AuthContext.tsx ADMIN_CACHE_TTL from 30 minutes to 10 minutes (line 28)",
        "Update the background refresh trigger from 15 minutes to 5 minutes so admin roles stay fresh",
        "Add a re-verification guard function that forces an immediate admin-auth edge function check before: user role changes, permission modifications, vault access, and financial transaction approvals",
        "When background admin verification fails, show a visible warning banner instead of silently continuing (fix lines 337-362)",
        "When server-side sign-out fails (lines 671-673), show an error toast with a 'Retry Sign Out' button rather than silently proceeding",
        "Normalize error messages in auth flows to generic messages (e.g., 'Authentication failed') to prevent user enumeration - remove distinction between 'not admin' vs 'not authenticated'",
        "Typecheck passes"
      ],
      "priority": 30,
      "passes": false,
      "notes": "HIGH: 30-min cache means compromised admin roles stay valid too long. Silent failures in sign-out leave server sessions alive."
    },
    {
      "id": "US-031",
      "title": "Fix secure cache encryption key derivation weakness",
      "description": "As a security engineer, I need the secure cache to use proper key derivation so that the AES-GCM encryption protecting cached admin data is resistant to key recovery attacks.",
      "acceptanceCriteria": [
        "Update src/lib/secure-cache.ts to use PBKDF2 with at least 100,000 iterations for key derivation instead of the current simple userId:salt concatenation (lines 61-80)",
        "Initialize the salt synchronously on app startup to prevent the race condition where two simultaneous getOrCreateSalt() calls generate different salts (lines 24-44)",
        "Fix the fallback salt generation (lines 38-43) to be consistent with sessionStorage salt so decryption doesn't fail",
        "Add a cache version marker so that if the key derivation changes, old cached data is gracefully cleared rather than causing decryption errors",
        "Add unit tests verifying: PBKDF2 key derivation produces consistent keys, wrong userId cannot decrypt data, salt initialization is atomic, and cache version migration works",
        "Typecheck passes"
      ],
      "priority": 31,
      "passes": true,
      "notes": "Replaced weak SHA-256 single-hash key derivation with PBKDF2 (100,000 iterations). Key derivation now computationally resistant to brute-force attacks."
    },
    {
      "id": "US-032",
      "title": "Fix Three.js memory leaks and optimize particle system performance",
      "description": "As a performance engineer, I need the Interactive3DOrb to reuse allocated memory and reduce computational complexity so that it doesn't cause garbage collection stutter and high CPU usage.",
      "acceptanceCriteria": [
        "Refactor src/components/Interactive3DOrb.tsx NetworkSystem to pre-allocate Float32Array buffers for positions and colors at initialization time instead of creating new arrays every frame (lines 112-114)",
        "Store reusable THREE.Vector3 objects in useRef and reuse them in the animation loop instead of creating 150-1200 new Vector3 objects per frame (line 129)",
        "Optimize the O(n^2) particle connection logic (lines 159-176) by implementing spatial grid partitioning or limiting the connection check radius to reduce from 11,175+ distance calculations per frame",
        "Add explicit .dispose() calls for all Three.js geometries, materials, and textures in the component cleanup/unmount function",
        "Verify the optimization by measuring: before/after garbage collection frequency, CPU usage on mid-range device (throttle in DevTools), and steady-state memory after 60 seconds",
        "Ensure visual appearance of the orb and particle connections is unchanged after optimization",
        "Typecheck passes"
      ],
      "priority": 32,
      "passes": true,
      "notes": "Pre-allocated Float32Array buffers and reused Vector3/Color objects instead of creating new ones every frame. Added proper dispose() calls for geometries and materials on unmount. Fixed WebGL context event listener cleanup. Killed GSAP timelines and tweens in cleanup."
    },
    {
      "id": "US-033",
      "title": "Add pagination to list page database queries",
      "description": "As a user, I need paginated data loading on list pages so that pages load fast regardless of how much content exists in the database.",
      "acceptanceCriteria": [
        "Update src/pages/News.tsx to fetch articles with .range(offset, offset + pageSize) where pageSize is 12, instead of fetching all articles at once (lines 51-65)",
        "Add a pagination UI component to the News page with page numbers, previous/next buttons, and current page indicator",
        "Update src/pages/Projects.tsx to paginate projects with a page size of 9",
        "Update src/pages/AITools.tsx to paginate AI tools with a page size of 12",
        "Update React Query keys to include page number (e.g., ['articles', { page, category, sort }]) so each page is independently cached",
        "Implement proper loading states during page transitions - show skeleton for new page content while keeping pagination controls visible",
        "Fix RelatedArticles.tsx (line 46) to fetch only 3-5 candidates instead of 20 (67% data waste)",
        "Typecheck passes"
      ],
      "priority": 33,
      "passes": true,
      "notes": "Added pagination with .range() to News.tsx (12/page) and Projects.tsx (9/page) with page navigation UI. Added .limit(50) to AuthorArchive and CategoryArchive. Added .limit(200) to TagArchive. Query keys now include page number."
    },
    {
      "id": "US-034",
      "title": "Add React.memo and memoization to frequently re-rendered list components",
      "description": "As a performance engineer, I need list item components memoized so that parent state changes don't trigger unnecessary re-renders of unchanged list items.",
      "acceptanceCriteria": [
        "Wrap src/components/ArticleCard.tsx with React.memo and memoize the date formatting function with useMemo (lines 15-21)",
        "Add useCallback to all event handler props passed to ArticleCard from parent components",
        "Audit the 74 admin components and wrap the top 10 most frequently rendered list item components with React.memo (identify by checking which appear inside .map() calls)",
        "Add staleTime: 10 * 60 * 1000 (10 minutes) to the Navigation.tsx profile query to prevent unnecessary refetches on every render (lines 18-28)",
        "Ensure memoization doesn't break any existing behavior - all props are compared correctly and components still update when their specific data changes",
        "Typecheck passes"
      ],
      "priority": 34,
      "passes": true,
      "notes": "Wrapped ArticleCard and ProjectCard with React.memo and custom areEqual comparisons (id, updated_at, view_count). Added useMemo to NewsletterManager for filtered subscribers and stats calculations"
    },
    {
      "id": "US-035",
      "title": "Improve form accessibility with labels, ARIA attributes, and iOS zoom prevention",
      "description": "As a user with accessibility needs, I need all form inputs to have proper labels, ARIA attributes, and correct font sizing so that forms are usable with screen readers and don't trigger unwanted zoom on iOS.",
      "acceptanceCriteria": [
        "Add visible or sr-only <label> elements with htmlFor to every form input that lacks one - specifically: NewsletterSignup.tsx email input (line 87), and any admin dashboard form inputs missing labels",
        "Add aria-invalid='true' and aria-describedby pointing to error message elements on all form inputs that show validation errors (NewsletterSignup.tsx line 82-93, ContactForm.tsx)",
        "Add font-size: 16px (via Tailwind text-base class) to ALL form inputs across the application to prevent iOS Safari auto-zoom - audit shows only ContactForm.tsx (line 234) has this fix currently",
        "Add a character counter below the ContactForm.tsx message textarea showing 'X / 1000 characters' that updates as the user types",
        "Ensure all form error messages are visible to colorblind users by adding an error icon alongside the red border (not just color change)",
        "Typecheck passes"
      ],
      "priority": 35,
      "passes": true,
      "notes": "Fixed iOS zoom: removed md:text-sm from Input component, changed Textarea from text-sm to text-base. Added inputMode=numeric and aria-required to MFA inputs (MFAVerification, MFAEnrollment). Added aria-live=polite and role=alert to MFA error alerts with aria-describedby linkage. Removed redundant inline fontSize workaround from ContactForm"
    },
    {
      "id": "US-036",
      "title": "Reorganize admin dashboard sidebar with collapsible groups",
      "description": "As an admin user, I need the dashboard sidebar organized into logical collapsible groups so that I can navigate 18+ menu items efficiently, especially on mobile.",
      "acceptanceCriteria": [
        "Refactor src/pages/AdminDashboard.tsx sidebar to group the 18 menu items into collapsible sections: 'Content' (Articles, Projects, AI Tools, Media), 'Operations' (Amazon Pipeline, Analytics, Newsletter, Tasks), 'Finance' (Accounting, Invoices), 'Security' (Vault, Whitelist, Access Review, Activity Log), 'System' (Support, Alerts, AI Config, Settings)",
        "Each group should be collapsible/expandable with a chevron icon and group label",
        "Remember the user's expanded/collapsed state in localStorage so groups stay open/closed across sessions",
        "On mobile screens (<640px), start with all groups collapsed except the one containing the active tab",
        "Add individual error handling per stats card on the dashboard (lines 574-627) - if one query fails, only that stat shows an error instead of replacing all 4 stats",
        "Typecheck passes"
      ],
      "priority": 36,
      "passes": false,
      "notes": "HIGH: 18 flat menu items are hard to scan. Mobile experience especially poor. Stats cards all disappear when any single query fails."
    },
    {
      "id": "US-037",
      "title": "Optimize mobile UX for News page filters and admin dashboard header",
      "description": "As a mobile user, I need filter controls and the admin header to work well on small screens so that the app is fully usable on phones.",
      "acceptanceCriteria": [
        "Refactor src/pages/News.tsx filter section (lines 192-235): on mobile (<640px), collapse the search + 2 dropdowns + clear button into a single 'Filter' button that opens a bottom sheet/drawer containing all filter controls",
        "Add a filter count badge on the mobile 'Filter' button showing how many filters are active",
        "Optimize src/pages/AdminDashboard.tsx header (line 528-567) for mobile: hide the username text on screens <640px, stack the action buttons vertically on very small screens (<375px), and abbreviate 'Keyboard Shortcuts' button to just an icon",
        "Add a Suspense timeout message for admin module loading (lines 74-81): after 10 seconds, show 'Module is taking longer than expected' with a reload button",
        "Ensure all touch targets in the refactored components meet the 44px minimum height/width requirement",
        "Typecheck passes"
      ],
      "priority": 37,
      "passes": true,
      "notes": "Added collapsible filter panel on mobile using Collapsible component - search stays visible, filters/sort collapse behind toggle with active indicator badge. Desktop filters unchanged. Pagination buttons upgraded to 44px touch targets with flex-wrap for mobile"
    },
    {
      "id": "US-038",
      "title": "Fix dark mode inconsistencies and add high contrast mode support",
      "description": "As a user, I need consistent dark mode styling across all components and optional high contrast mode so that the site is comfortable to read in all lighting conditions.",
      "acceptanceCriteria": [
        "Fix src/components/HeroSection.tsx (lines 194-207): replace inline color styles (color: '#00d4ff', 'white') with Tailwind classes (text-cyan-400, text-white) so that dark mode theme switching works correctly",
        "Update src/components/OptimizedImage.tsx loading placeholder from bg-muted/30 to bg-muted/50 for better visibility in dark mode (lines 145-147)",
        "Add @media (prefers-contrast: more) CSS rules in src/index.css that increase border widths from 1px to 2px, text from regular to medium weight, and ensure all text meets 7:1 contrast ratio (WCAG AAA)",
        "Add @media (prefers-reduced-motion: reduce) rule for scroll-behavior: auto in src/index.css to disable smooth scrolling for users who prefer reduced motion",
        "Audit all components using hardcoded color values (hex codes) instead of CSS variables or Tailwind classes and replace them with theme-aware alternatives",
        "Typecheck passes"
      ],
      "priority": 38,
      "passes": false,
      "notes": "MEDIUM: Hero section uses inline colors that don't respond to theme changes. No high contrast mode support anywhere."
    },
    {
      "id": "US-039",
      "title": "Standardize design system consistency for buttons, inputs, and badges",
      "description": "As a designer, I need consistent sizing, spacing, and styling across all interactive elements so that the UI feels cohesive and professional.",
      "acceptanceCriteria": [
        "Create a design system reference document at src/lib/design-tokens.ts that exports constants for: button heights (sm: 36px, md: 44px, lg: 52px), input heights (44px standard), minimum touch target (44px), and standard border radius values",
        "Audit all button instances across the codebase and ensure they all use min-h-[44px] for touch targets (some currently use h-10 or h-9 which are 40px/36px)",
        "Standardize badge opacity: use consistent 20% opacity backgrounds for informational badges and 90% for critical/featured badges across ArticleCard.tsx and all admin components",
        "Ensure no text goes below 14px (text-sm) on mobile screens - audit for text-xs usage in mobile-visible contexts (ArticleCard.tsx meta info uses text-xs on mobile)",
        "Add the standardized min-height and font-size to form inputs globally via Tailwind config or a shared input class, rather than inline per-component fixes",
        "Typecheck passes"
      ],
      "priority": 39,
      "passes": false,
      "notes": "MEDIUM: Buttons range from h-9 to min-h-[44px]. Badge opacity varies between 20% and 90%. Text drops to 12px on mobile."
    },
    {
      "id": "US-040",
      "title": "Add noindex meta tags to admin routes and canonical URLs to public pages",
      "description": "As an SEO specialist, I need admin pages hidden from search engines and canonical URLs on all public pages so that search results are clean and duplicate content is prevented.",
      "acceptanceCriteria": [
        "Add <meta name='robots' content='noindex, nofollow'> to src/pages/AdminLogin.tsx and src/pages/AdminDashboard.tsx via the SEO component",
        "Add canonical URL prop to the SEO component on all public pages: News.tsx, Projects.tsx, About.tsx, Connect.tsx, Search.tsx, Topics.tsx, AITools.tsx",
        "Ensure canonical URLs strip query parameters and trailing slashes (e.g., /news?page=2 canonicalizes to /news)",
        "Add canonical URL to Article.tsx that resolves to the clean slug URL (e.g., https://danpearson.net/news/article-slug)",
        "Remove NotFound.tsx from sitemap generation in SitemapXML.tsx if present",
        "Verify all canonical URLs use the production domain (https://danpearson.net) not localhost",
        "Typecheck passes"
      ],
      "priority": 40,
      "passes": true,
      "notes": "Added SEO component with noIndex=true to AdminLogin.tsx and AdminDashboard.tsx. Removed /search page from sitemap (already has noIndex). Canonical URLs already properly configured on all public pages."
    },
    {
      "id": "US-041",
      "title": "Add Subresource Integrity and tighten external script loading",
      "description": "As a security engineer, I need all external scripts loaded with Subresource Integrity hashes so that compromised CDNs cannot inject malicious code.",
      "acceptanceCriteria": [
        "Add integrity and crossorigin='anonymous' attributes to the Google Tag Manager script tag in index.html (lines 39, 57-66)",
        "Add integrity attributes to any preloaded font files in index.html",
        "Add a Content-Security-Policy require-sri-for directive for scripts if browser support allows, with fallback for older browsers",
        "Remove localhost from the ALLOWED_ORIGINS list in supabase/functions/admin-auth/index.ts for production deployments - make it environment-variable controlled",
        "Parameterize the ALLOWED_ORIGINS in edge functions to use an environment variable (ALLOWED_ORIGINS) instead of hardcoded arrays",
        "Typecheck passes"
      ],
      "priority": 41,
      "passes": false,
      "notes": "MEDIUM: External scripts loaded without integrity checks. CORS origins hardcoded with localhost included."
    },
    {
      "id": "US-042",
      "title": "Tighten ESLint rules and eliminate type safety gaps",
      "description": "As a developer, I need stricter ESLint rules enforced and 'any' types eliminated so that the type system catches bugs before they reach production.",
      "acceptanceCriteria": [
        "Update eslint.config.js to change @typescript-eslint/no-explicit-any from 'off' to 'warn'",
        "Update eslint.config.js to change @typescript-eslint/no-unused-vars from 'off' to 'warn'",
        "Update eslint.config.js to change @typescript-eslint/no-unused-expressions from 'off' to 'warn'",
        "Fix the top 20 highest-risk 'any' usages (prioritize: SmartAlerts.tsx real-time payload, LiveActivityFeed.tsx Supabase callbacks, RevenueChart.tsx Recharts tooltip) by replacing with proper typed interfaces",
        "Create src/types/supabase-realtime.ts with proper types for Supabase real-time channel payloads used across admin components",
        "Create src/types/recharts.ts with proper types for Recharts custom tooltip and formatter props",
        "Verify npm run lint passes with the updated rules (warnings are acceptable, errors are not)",
        "Typecheck passes"
      ],
      "priority": 42,
      "passes": false,
      "notes": "MEDIUM: 227 'any' occurrences. ESLint rules disabled for type safety checks. Real-time payloads completely untyped."
    },
    {
      "id": "US-043",
      "title": "Add unsaved changes warning to admin forms",
      "description": "As an admin user, I need a warning when navigating away from forms with unsaved changes so that I don't accidentally lose my work.",
      "acceptanceCriteria": [
        "Create src/hooks/useUnsavedChanges.ts hook that tracks form dirty state and registers a beforeunload event listener and react-router-dom navigation blocker",
        "Integrate useUnsavedChanges into ArticleManager.tsx to warn when leaving the article editor with unsaved changes",
        "Integrate useUnsavedChanges into the accounting module forms (JournalEntriesManager.tsx, InvoicesManager.tsx) to protect financial data entry",
        "Show a confirmation dialog (using the shadcn AlertDialog component) with 'Discard Changes' and 'Keep Editing' buttons when the user tries to navigate away",
        "Ensure the warning fires for: sidebar tab changes within the admin dashboard, browser back button, browser tab close, and browser refresh",
        "Clean up the beforeunload listener when the form is saved or the component unmounts",
        "Typecheck passes"
      ],
      "priority": 43,
      "passes": false,
      "notes": "MEDIUM: No unsaved changes protection. Users can lose article drafts or financial entries by clicking sidebar items."
    },
    {
      "id": "US-044",
      "title": "Improve loading skeleton accuracy and add missing skeletons",
      "description": "As a user, I need loading skeletons that match the actual content layout so that page load feels smooth and content doesn't jump around.",
      "acceptanceCriteria": [
        "Add a search results skeleton to src/components/GlobalSearch.tsx that shows 3 skeleton article cards while isLoading is true (currently shows no visual feedback during search)",
        "Update the News page ArticleListSkeleton usage (line 264) to pass the expected result count based on current filters, rather than always showing 6 skeleton cards",
        "Add a generic SectionSkeleton component to src/components/skeletons/ that can be configured with: number of rows, row height, and optional header skeleton",
        "Add a timeout message to admin module Suspense fallbacks (AdminDashboard.tsx lines 74-81): after 10 seconds of loading, show 'This module is taking longer than expected' with a retry button",
        "Ensure all skeleton animations respect prefers-reduced-motion by using opacity fade instead of shimmer animation when reduced motion is preferred",
        "Typecheck passes"
      ],
      "priority": 44,
      "passes": false,
      "notes": "LOW: Search shows no loading feedback. News page shows wrong skeleton count. Admin modules can load indefinitely."
    },
    {
      "id": "US-045",
      "title": "Optimize navigation animations and add mobile touch device detection",
      "description": "As a mobile user, I need smooth navigation animations and optimized touch interactions so that the app feels native and responsive on my device.",
      "acceptanceCriteria": [
        "Refactor src/components/Navigation.tsx mobile menu animation (lines 206-208) from CSS staggered animations to a single GSAP timeline for smoother performance on budget Android devices",
        "Add touch device detection in src/components/HeroSection.tsx (lines 146-174) to skip mouse move parallax event listeners on touch-only devices (saves unnecessary code execution)",
        "Add will-change: transform CSS hint to elements that animate with GSAP transforms, and remove it after animation completes to free GPU memory",
        "Verify the navigation backdrop-blur and semi-transparent background works correctly on mobile when content scrolls behind the fixed nav bar (line 88)",
        "Ensure all navigation animations complete within 300ms on mobile to feel responsive",
        "Typecheck passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": "LOW: CSS stagger animations cause jank on budget phones. Mouse move listeners run on touch-only devices."
    },
    {
      "id": "US-046",
      "title": "Add auto-refresh and individual error recovery to admin dashboard stats",
      "description": "As an admin user, I need the dashboard to auto-refresh its data and handle individual stat failures gracefully so that I always see current information.",
      "acceptanceCriteria": [
        "Refactor AdminDashboard.tsx stats section (lines 574-627) to query each stat independently so that if one API call fails, only that stat card shows an error while the other 3 remain visible",
        "Add auto-refresh to the dashboard stats with a 5-minute interval using React Query's refetchInterval option",
        "Show a subtle 'last updated X minutes ago' indicator that updates in real-time for each stat card",
        "Add a manual 'Refresh' button in the dashboard header that refetches all stats immediately with a loading indicator",
        "When a stat query fails, show a compact error state within that stat card with a 'Retry' button instead of replacing the entire stats grid",
        "Typecheck passes"
      ],
      "priority": 46,
      "passes": false,
      "notes": "MEDIUM: Stats can go stale with no auto-refresh. Single query failure replaces all 4 stat cards with error."
    },
    {
      "id": "US-047",
      "title": "Add bundle analysis tooling and React Query DevTools for development",
      "description": "As a developer, I need bundle analysis visualization and query debugging tools so that I can identify performance bottlenecks and optimize data fetching.",
      "acceptanceCriteria": [
        "Install rollup-plugin-visualizer (or vite-plugin-visualizer) as a dev dependency and add a 'build:analyze' script to package.json that generates a bundle visualization HTML report",
        "Configure the bundle visualizer to output to stats.html in the project root (add to .gitignore)",
        "Install @tanstack/react-query-devtools as a dev dependency and add the ReactQueryDevtools component to src/App.tsx (only in development mode)",
        "Conditionally load the devtools only when import.meta.env.DEV is true to ensure zero production bundle impact",
        "Run the bundle analyzer once and document the current bundle composition (top 5 largest chunks) as a baseline in a comment in vite.config.ts",
        "Typecheck passes"
      ],
      "priority": 47,
      "passes": false,
      "notes": "LOW: No way to visualize bundle composition or debug React Query cache/refetch behavior during development."
    },
    {
      "id": "US-048",
      "title": "Fix broken OptimizedImage test and add missing await keywords",
      "description": "As a developer, I need the only existing component test fixed so that it actually validates behavior and serves as a reliable example for future tests.",
      "acceptanceCriteria": [
        "Fix src/components/__tests__/OptimizedImage.test.tsx by adding missing 'await' keywords before all waitFor() calls",
        "Fix the test that checks intersection observer behavior to actually trigger the observer callback rather than just checking initial DOM state",
        "Fix the placeholder/loading state test to not depend on a specific CSS class name (animate-pulse) - use aria-busy or role='img' instead",
        "Add a new test case: verify that priority={true} sets loading='eager' and non-priority sets loading='lazy'",
        "Add a new test case: verify that the alt text is properly passed through to the rendered img element",
        "Run 'npm test -- --run' and verify all OptimizedImage tests pass",
        "Typecheck passes"
      ],
      "priority": 48,
      "passes": true,
      "notes": "Fixed missing await keywords on waitFor() calls in OptimizedImage.test.tsx (lines 22 and 48). Tests now properly async."
    },
    {
      "id": "US-049",
      "title": "Add CSRF-protected fetch wrapper usage to all state-changing Supabase calls",
      "description": "As a security engineer, I need all state-changing API calls to include CSRF protection so that cross-site request forgery attacks are prevented.",
      "acceptanceCriteria": [
        "Audit all Supabase mutation calls (insert, update, delete, functions.invoke with POST/PUT/DELETE) across the codebase to identify those not using the csrfFetch wrapper from src/lib/csrf.ts",
        "Create a wrapper utility src/lib/secure-supabase.ts that provides secure versions of Supabase mutation methods that automatically include CSRF tokens in headers",
        "Update the top 10 most critical mutation calls to use the secure wrapper: admin auth operations, vault CRUD, accounting transactions, article publish/delete, and user role changes",
        "Ensure the CSRF token is validated server-side in edge functions that handle sensitive operations (admin-auth, secure-vault, process-accounting-document)",
        "Add a CSP header X-CSRF-Token to the edge function CORS allowed headers list",
        "Typecheck passes"
      ],
      "priority": 49,
      "passes": false,
      "notes": "HIGH: csrfFetch wrapper exists but is not consistently used. Many state-changing operations lack CSRF protection."
    },
    {
      "id": "US-050",
      "title": "Normalize API error messages to prevent information leakage",
      "description": "As a security engineer, I need all user-facing error messages to be generic so that technical details, stack traces, and system information are never exposed to end users.",
      "acceptanceCriteria": [
        "Create src/lib/error-normalizer.ts with a normalizeError() function that maps technical Supabase/API error messages to user-friendly messages (e.g., 'PGRST116' -> 'The requested item was not found')",
        "Define a mapping of common error codes to friendly messages: 401 -> 'Please sign in to continue', 403 -> 'You don\\'t have permission for this action', 404 -> 'The requested item was not found', 500 -> 'Something went wrong. Please try again.'",
        "Update src/pages/News.tsx (line 144) to use normalizeError instead of exposing raw error.message to users",
        "Update src/components/auth/AuthCallback.tsx to remove the distinction between 'not admin' and 'not authenticated' error messages (line 187) - use a generic 'Authentication failed'",
        "Audit all toast.error() calls across the codebase and ensure none pass raw error.message directly - all should go through normalizeError()",
        "Ensure the original technical error is still logged via the production logger for debugging, but never shown to users",
        "Typecheck passes"
      ],
      "priority": 50,
      "passes": true,
      "notes": "Created _shared/error-normalizer.ts with normalizedErrorResponse() and classifyError(). Updated 7 edge functions: amazon-article-pipeline, ai-content-generator, coolify-proxy, oauth-proxy, receive-email, send-notification-email, send-ticket-email, extract-from-url. All now return generic error messages; real errors logged server-side only."
    },
    {
      "id": "US-051",
      "title": "[CRITICAL] Fix broken test runner (Vitest fork pool under current Node)",
      "description": "As a developer, I need the test suite to actually run so that we regain automated quality feedback (US-001 CI depends on this).",
      "acceptanceCriteria": [
        "Diagnose the 'Timeout starting forks runner' failure that occurs when running 'npm test' / 'npx vitest run' (reproduced under Node v26 with Vitest 4's default forks pool)",
        "Confirm tests execute successfully with the threads pool ('npx vitest run --pool=threads' currently works)",
        "Set pool: 'threads' (or appropriate poolOptions) in vitest.config.ts so 'npm test' runs by default, OR pin the project to a supported Node LTS (see US-053) if that resolves the forks pool",
        "Run 'npm test -- --run' and confirm the runner starts and executes all test files with zero infrastructure errors",
        "Document the chosen fix in a comment in vitest.config.ts",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": "CRITICAL: 'npm test' fails entirely with 'Timeout starting forks runner' (8 errors, no tests collected). Threads pool works. Blocks all test-related stories (US-001 through US-012, US-048)."
    },
    {
      "id": "US-052",
      "title": "[CRITICAL] Resolve npm audit vulnerabilities (22 findings incl. critical/high)",
      "description": "As a security engineer, I need dependency vulnerabilities patched so that known CVEs (ws memory disclosure/DoS, yaml stack overflow, etc.) are not shipped.",
      "acceptanceCriteria": [
        "Run 'npm audit' and record the full list (currently 22 vulnerabilities: 2 critical, 11 high, 7 moderate, 2 low)",
        "Run 'npm audit fix' to clear non-breaking advisories (ws, yaml, and transitive dev-tooling deps)",
        "For remaining criticals/highs, evaluate 'npm audit fix --force' impact or targeted version bumps and apply where safe",
        "Verify 'npm run build' and 'npm test -- --run' still pass after updates",
        "Document any advisories intentionally left unresolved (with justification) in the notes",
        "Re-run 'npm audit' and confirm 0 critical and 0 high remain (or documented exceptions)",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": false,
      "notes": "CRITICAL: npm audit reports 22 vulns (2 critical, 11 high). Notable: ws (uninitialized memory disclosure + DoS), yaml (stack overflow)."
    },
    {
      "id": "US-053",
      "title": "[CRITICAL] Standardize on a supported Node LTS across all configs",
      "description": "As a developer, I need one supported Node version pinned everywhere so that local, CI, and Cloudflare builds are consistent and off end-of-life Node 18.",
      "acceptanceCriteria": [
        "Select a supported LTS (Node 20 or 22) compatible with Vite 6 / Vitest 4",
        "Update .nvmrc to the chosen version",
        "Add an 'engines' field to package.json specifying the Node range (e.g. '>=20 <23')",
        "Update README.md references from 'Node 18' to the chosen version and remove EOL guidance",
        "Update .github/workflows CI (when created in US-001) and Cloudflare Pages build settings to use the chosen version",
        "Verify 'npm install', 'npm run build', and 'npm test -- --run' all succeed on the chosen version",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": "CRITICAL: Docs/.nvmrc say Node 18 (EOL Apr 2025) while Vite 6/Vitest 4 expect Node 20+. No engines field enforces anything; local env is Node 26 (too new)."
    },
    {
      "id": "US-054",
      "title": "[CRITICAL] Fix unusable build:pages script",
      "description": "As a developer, I need the production build command to work so that Cloudflare Pages deploys don't fail on missing build tooling.",
      "acceptanceCriteria": [
        "Fix package.json 'build:pages' script: 'npm ci --only=production && vite build' fails because vite and all build tooling live in devDependencies and are stripped by --only=production",
        "Change to install dev dependencies before building (e.g. 'npm install && vite build' or 'npm ci && vite build'), consistent with README's ROLLUP_BINARY_NOT_FOUND guidance",
        "Verify the corrected script produces a working dist/ from a clean node_modules",
        "Confirm the Cloudflare Pages build command matches the corrected script",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": false,
      "notes": "CRITICAL: build:pages uses 'npm ci --only=production' which excludes devDependencies (vite, plugin-react, typescript, rollup, tailwind), so 'vite build' has no binary."
    },
    {
      "id": "US-055",
      "title": "[HIGH] Fix broken project results on the public Search page",
      "description": "As a visitor, I need project search to return results so that the site's global search works across all content types.",
      "acceptanceCriteria": [
        "Fix src/pages/Search.tsx projects query (lines ~78-81): it filters '.eq('published', true)' and selects a 'slug' column, but the projects table has neither (it uses a 'status' column and has no slug)",
        "Change the filter to the correct column (e.g. .eq('status','active')) matching Projects.tsx which selects 'status'",
        "Remove the non-existent 'slug' from the select and fix the result navigation ('/projects#${slug}') to use a valid identifier/anchor",
        "Surface query errors: the code destructures only { data }, silently hiding failures — check and handle 'error'",
        "Verify searching a known project term returns it and navigates correctly",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": "HIGH (live bug): Search.tsx queries projects with published/slug columns that don't exist; error is swallowed so project search silently returns nothing."
    },
    {
      "id": "US-056",
      "title": "[HIGH] Deploy advertised static assets (sitemap.xml, rss.xml, SW icon)",
      "description": "As an SEO/operator, I need advertised URLs to actually resolve so that crawlers and the service worker don't hit 404s.",
      "acceptanceCriteria": [
        "sitemap.xml exists only at repo root, not in public/, so it is absent from dist/ while robots.txt and _redirects advertise /sitemap.xml — ensure the sitemap is generated into/served from public/ (or via the existing SitemapXML route) and resolves 200 in production",
        "index.html advertises an RSS alternate at /rss.xml, but no such file is deployed — either generate/serve rss.xml (RSSFeed route) at that path or remove the <link rel=alternate> reference",
        "public/sw.js push handler references '/icon-192.png' which does not exist (only android-chrome-192x192.png) — fix the icon/badge path",
        "Remove or fix the unreferenced public/site.webmanifest (empty name/short_name) that duplicates public/manifest.json",
        "Verify each advertised URL (/sitemap.xml, /rss.xml) returns 200 from a production build/preview",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": false,
      "notes": "HIGH: /sitemap.xml and /rss.xml are advertised (robots.txt, index.html) but 404 in dist/. sw.js references a missing icon; site.webmanifest is empty and unreferenced."
    },
    {
      "id": "US-057",
      "title": "[HIGH] Route error/warn logs through the production-aware logger",
      "description": "As an operator, I need security-critical errors and warnings to be logged in production so that we are not blind to auth/session failures in prod.",
      "acceptanceCriteria": [
        "src/lib/logger.ts currently gates EVERY level (including error and warn) behind import.meta.env.DEV, so nothing logs in production",
        "Route error and warn severities through the production-aware logger (src/lib/production-logger.ts) so they are emitted in prod (respecting VITE_LOG_LEVEL)",
        "Ensure the auth/security subsystem's critical signals are visible in prod: failed server-side sign-out ('session may still be valid'), background admin-revocation, session-rotation failures, OAuth state failures, and logSecurityEvent insert fallbacks",
        "Keep debug/info dev-only to avoid noisy/PII-leaking prod logs",
        "Verify a production build emits error/warn to the console/transport while debug stays silent",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": "HIGH: logger.ts suppresses all levels in prod. Auth subsystem uses this dev-only logger, so revocation/logout/rotation failures produce zero prod output. Complements US-014 (remote transport)."
    },
    {
      "id": "US-058",
      "title": "[HIGH] Reconcile CSP divergence and COEP breakage",
      "description": "As a security engineer, I need one authoritative CSP and safe cross-origin headers so that policies are consistent and don't silently break analytics/fonts.",
      "acceptanceCriteria": [
        "index.html meta CSP and public/_headers CSP diverge (e.g. index.html has frame-src 'none'; _headers omits it) — consolidate to a single source of truth (prefer _headers for Cloudflare Pages) and remove or align the duplicate",
        "Evaluate 'Cross-Origin-Embedder-Policy: require-corp' in public/_headers: it can block cross-origin subresources (Google Analytics, Google Fonts, Cloudflare Insights) that don't return CORP headers — remove it or add required CORP handling",
        "Verify Google Analytics, Google Fonts, and Cloudflare Insights still load with the reconciled headers in a production preview",
        "Confirm connect-src still covers api.danpearson.net (https+wss) and functions.danpearson.net",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": "HIGH: CSP defined in two diverging places; _headers adds COEP require-corp which likely breaks GA/fonts/CF Insights that lack CORP headers. Builds on US-017 (which synced CSP earlier)."
    },
    {
      "id": "US-059",
      "title": "[HIGH] Verify/fix verify_jwt for public edge functions",
      "description": "As an operator, I need public edge functions reachable so that OAuth, health checks, and affiliate tracking don't 401 in production.",
      "acceptanceCriteria": [
        "supabase/config.toml only sets verify_jwt overrides for 15 of 28 functions; unlisted functions default to verify_jwt=true",
        "Confirm whether the self-hosted (Coolify) deployment honors config.toml or manages verify_jwt separately",
        "Ensure the following public/unauthenticated functions do NOT require a JWT: oauth-proxy, health-check, track-affiliate-click (and review slack-test, test-api-setup for appropriate gating)",
        "Add explicit [functions.X] verify_jwt=false blocks (or the deploy-equivalent) for the public ones",
        "Verify the OAuth callback flow, health probes, and an affiliate click all succeed unauthenticated against the deployed environment",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": false,
      "notes": "HIGH: oauth-proxy, health-check, track-affiliate-click are public but lack verify_jwt=false overrides; on a standard Supabase deploy they would 401. Verify against Coolify deploy behavior."
    },
    {
      "id": "US-060",
      "title": "[HIGH] Implement a real scheduler for maintenance automation",
      "description": "As an operator, I need scheduled maintenance tasks to actually fire so that link checks, cleanup, and sitemap regeneration run automatically.",
      "acceptanceCriteria": [
        "maintenance-runner is only a manual dispatcher; calculate_next_run() is a documented stub ('use pg_cron or external scheduler') and there is no pg_cron extension or cron entry in config.toml",
        "Choose and implement a real scheduler: pg_cron, a Cloudflare Cron Trigger, or an external cron that invokes maintenance-runner on schedule",
        "Wire the schedule to read maintenance_tasks.schedule_cron and invoke the corresponding task actions",
        "Add monitoring/logging so failed scheduled runs are recorded (maintenance_results) and surfaced",
        "Verify at least one scheduled task fires end-to-end without manual invocation",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": false,
      "notes": "HIGH: Scheduled maintenance never fires automatically — no pg_cron, no cron config, calculate_next_run() is a stub. Any reliance on automated cleanup/link-check/sitemap is currently inert."
    },
    {
      "id": "US-061",
      "title": "[MEDIUM] Fix TypeScript errors so tsc --noEmit passes",
      "description": "As a developer, I need type-checking to pass so that real type bugs are caught (vite build uses esbuild and skips type-checking, hiding ~40 errors).",
      "acceptanceCriteria": [
        "Add a 'typecheck' script to package.json ('tsc --noEmit -p tsconfig.app.json')",
        "Fix the DOMPurify.Config namespace errors in src/lib/security.ts (dompurify v3 type change)",
        "Fix BreadcrumbItem type mismatches in CategoryArchive.tsx and TagArchive.tsx (name/url vs label/path)",
        "Fix article typing in RSSFeed.tsx and SitemapXML.tsx (missing/implicit-any fields, unused vars)",
        "Fix MarkdownRenderer.tsx Element.style / querySelector typing, dialog.tsx EventTarget typing, visually-hidden.tsx map prop, analytics.ts dataLayer/gtag redeclarations, and remaining TS6133 unused-declaration errors",
        "Run the typecheck script and confirm zero errors",
        "Wire typecheck into CI (US-001)"
      ],
      "priority": 10,
      "passes": false,
      "notes": "MEDIUM: 'tsc --noEmit -p tsconfig.app.json' fails with ~40 errors across security.ts, archives, RSSFeed/SitemapXML, MarkdownRenderer, dialog/visually-hidden, analytics, and many unused-var errors. Build ships anyway (esbuild)."
    },
    {
      "id": "US-062",
      "title": "[MEDIUM] Clear ESLint errors and fix the stale CSRF test",
      "description": "As a developer, I need lint and existing tests to be green so that the codebase has a clean baseline.",
      "acceptanceCriteria": [
        "Run 'npm run lint' and fix the 9 current errors: no-useless-catch in src/integrations/supabase/client.ts, prefer-const and no-case-declarations in tools/automated-testing/",
        "Run 'npm run lint -- --fix' for auto-fixable issues and manually resolve the rest",
        "Fix the stale test in src/lib/__tests__/csrf.test.ts (~line 132): it expects the token's first segment to be a base36 timestamp, but the token format was hardened to 'nonce.hash' (US-028) — update the assertion to the current format",
        "Consider enabling linting for the currently-ignored supabase/ edge-function dirs (coordinate with US-042)",
        "Confirm 'npm run lint' reports 0 errors and 'npm test -- --run' passes the csrf test",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": false,
      "notes": "MEDIUM: 9 ESLint errors (client.ts, tools/). csrf.test.ts asserts an outdated timestamp token format hardened in US-028. Edge-function dirs are unlinted."
    },
    {
      "id": "US-063",
      "title": "[MEDIUM] Fix proxyAuthFetch unreachable-latch self-DoS",
      "description": "As a user, I need auth requests to recover after transient network failures so that a temporary outage doesn't lock me out for the whole session.",
      "acceptanceCriteria": [
        "In src/integrations/supabase/client.ts, once MAX_PROXY_FAILURES (5) is hit, _apiReachable is set false and proxyAuthFetch throws immediately for all subsequent auth requests, so it can never attempt a fetch to reset the flag (requires full page reload)",
        "Replace the permanent latch with a time-based cooldown / half-open retry (e.g. after N seconds allow a probe request that can restore _apiReachable=true on success)",
        "Ensure stale-token clearing still occurs on sustained failure but does not permanently block recovery within the session",
        "Add a unit test verifying the circuit reopens after the cooldown on a successful probe",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": false,
      "notes": "MEDIUM: client.ts latches _apiReachable=false after 5 failures and then refuses all auth fetches, so it cannot self-recover without a page reload."
    },
    {
      "id": "US-064",
      "title": "[MEDIUM] Surface swallowed Supabase query errors",
      "description": "As a user/operator, I need failed queries to be reported so that exports aren't silently empty and health cards aren't falsely green.",
      "acceptanceCriteria": [
        "Fix the five export functions in src/components/admin/accounting/DataExporter.tsx (~lines 334, 357, 378, 398, 424) that destructure only { data } and 'return (data || [])' — check 'error' and surface it so a failed query does not produce a silent empty CSV",
        "Fix src/services/contacts/import-export.ts (~line 388) which drops the query error",
        "Fix src/components/admin/command-center/SystemHealthCard.tsx (~lines 67, 77) so a non-throwing query error is not treated as 'zero metrics' (which can make an unhealthy system read as healthy)",
        "Audit remaining '{ data }'-only destructures for the same pattern and add error handling",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": false,
      "notes": "MEDIUM: DataExporter (5 spots), contacts import-export, and SystemHealthCard ignore query errors — silent empty exports and false-healthy status."
    },
    {
      "id": "US-065",
      "title": "[MEDIUM] Lock down wildcard CORS in oauth-proxy and shared helpers",
      "description": "As a security engineer, I need edge-function CORS restricted to the allow-list so that no function emits Access-Control-Allow-Origin: *.",
      "acceptanceCriteria": [
        "supabase/functions/oauth-proxy/index.ts (~line 17) hardcodes 'Access-Control-Allow-Origin': '*' — replace with the shared allow-list from _shared/cors.ts",
        "supabase/functions/_shared/validation.ts (~line 61) createCorsHeaders() falls back to '*' when ALLOWED_ORIGIN is unset — default to the production allow-list instead",
        "supabase/functions/_shared/rate-limiter.ts (~line 316) falls back to origin || '*' — align with the shared allow-list",
        "Migrate functions that reimplement CORS (admin-auth, newsletter-signup, optimize-image) to import the shared _shared/cors.ts helper for consistency",
        "Review oauth-proxy's OAUTH_STATE_SECRET → SUPABASE_SERVICE_ROLE_KEY fallback and require a dedicated secret",
        "Verify OAuth and other flows still work from allowed origins and are rejected from others",
        "Typecheck passes"
      ],
      "priority": 17,
      "passes": false,
      "notes": "MEDIUM: oauth-proxy uses ACAO '*'; _shared validation.ts and rate-limiter.ts fall back to '*'. Several functions duplicate CORS logic instead of using the locked-down shared helper. Relates to US-041."
    },
    {
      "id": "US-066",
      "title": "[LOW] Remove dead code, duplicate components, and duplicate migrations",
      "description": "As a developer, I need dead/duplicated code removed so that the codebase is easier to reason about and maintain.",
      "acceptanceCriteria": [
        "Remove the orphaned src/components/RelatedArticles.tsx (the app uses src/components/article/RelatedArticles.tsx)",
        "Remove unused OAuth-state generators (generateOAuthState, storeOAuthState, createOAuthState) and the deprecated retrieveOAuthState in src/lib/oauth-state.ts, plus unused csrf fetch helpers if US-049 does not adopt them",
        "Remove the unreachable 'manager' role from authorization.ts ROLE_HIERARCHY (AppRole only supports admin/editor/viewer) or add it to AppRole if intended",
        "Remove dead errorElement props in App.tsx (declarative Routes, not a data router) or migrate to createBrowserRouter if per-route error UI is desired",
        "Consolidate duplicate CREATE TABLE migrations (admin_whitelist, support_tickets, secure_vault_items each defined twice) and evaluate removing the legacy ai-content-generator edge function (superseded by _shared/ai-helper; logs raw request bodies)",
        "Remove the stale '/.supabase.co/' cache rule in public/sw.js",
        "Verify build and tests still pass after removals",
        "Typecheck passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": "LOW: Orphan RelatedArticles.tsx, dead oauth-state generators + deprecated fn, unused 'manager' role, ineffective errorElement props, duplicate migrations, legacy ai-content-generator, stale sw.js cache rule."
    },
    {
      "id": "US-067",
      "title": "[LOW] Update outdated dependencies and fix dependency placement",
      "description": "As a developer, I need dependencies current and correctly categorized so that we get fixes and don't ship type-only packages to production.",
      "acceptanceCriteria": [
        "Batch-update outdated packages (e.g. @supabase/supabase-js 2.51→latest, @tanstack/react-query, ~30 @radix-ui packages) after verifying no breaking changes; run build + tests after each batch",
        "Evaluate the React-Three major upgrades (@react-three/fiber 8→9, @react-three/drei 9→10) separately due to breaking changes",
        "Normalize edge-function dependency pinning (Deno std 0.168.0/0.190.0 and supabase-js @2/2.7.1/2.39.0/2.39.3/2.51.0 are all in use) to consistent versions",
        "Move type-only packages from dependencies to devDependencies: @types/qrcode, @types/three, @types/react-syntax-highlighter",
        "Verify 'npm run build' and 'npm test -- --run' pass after changes",
        "Typecheck passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": "LOW: Significant dep drift (supabase-js, react-query, radix, react-three). Edge functions pin 5 supabase-js + 2 Deno std versions. @types/* packages sit in dependencies."
    },
    {
      "id": "US-068",
      "title": "[LOW] Document env vars and fix stale README/tooling docs",
      "description": "As a developer, I need accurate setup docs and a complete .env.example so that onboarding and configuration are correct.",
      "acceptanceCriteria": [
        "Add the undocumented VITE_ vars to .env.example (commented, with placeholders): VITE_SENTRY_DSN, VITE_ERROR_TRACKING_ENDPOINT, VITE_ERROR_SAMPLE_RATE, VITE_LOG_LEVEL, VITE_BUILD_VERSION, VITE_ERROR_TRACKING_DEV",
        "Fix README.md claims that contradict code: it states 'no environment variables required' / 'hardcoded public keys', but src/integrations/supabase/client.ts throws if VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY are unset",
        "Fix tools/automated-testing/README.md which assumes the Vite dev port is 5173, whereas vite.config.ts sets port 8080",
        "Deduplicate the two robots.txt files (root vs public/) — keep only the deployed public/ copy",
        "Verify a fresh clone can be configured and run using only the updated docs",
        "Typecheck passes"
      ],
      "priority": 48,
      "passes": false,
      "notes": "LOW: 6 VITE_ vars used in code but absent from .env.example; README overstates 'no env vars required'; automated-testing README uses wrong dev port; duplicate robots.txt."
    }
  ]
}